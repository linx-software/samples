name: Latest YouTube Videos
on:
  schedule:
  #runs every hr
    - cron: '0 1 * * *'
  workflow_dispatch:
  
jobs:
  update-readme-with-youtube:
    name: Update this repo's REAME with the latest videos from YouTube
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          comment_tag_name: "YOUTUBE-VIDEOS-LIST"
          feed_list: "https://www.youtube.com/feeds/videos.xml?channel_id=UCO4KWEv8nUzeaFRO4zKS4gA"
  
